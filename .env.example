# Core database credentials
DATABASE_URL=postgresql://badge_user:badge_pass@localhost:5432/badge_db

# Basic authentication for /get-work endpoint
WORK_BASIC_AUTH_USERNAME=queue_worker
WORK_BASIC_AUTH_PASSWORD=super-secret-password

# Optional pool tuning (defaults: 1/10)
DB_POOL_MIN_SIZE=1
DB_POOL_MAX_SIZE=10
